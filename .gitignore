node_modules/
npm-debug.log*
.DS_Store
*.tgz
.wiki/_site/

# compiled output (built on publish via prepublishOnly; shipped via package "files")
dist/

# wikify:start (managed - edits inside this block are overwritten)
# Generated wiki mirror (regenerated by `wikify build`; do not commit)
.wiki/_site/
.wiki/.build-cache.json
.wiki/.imported-prs.json
.wiki/_imported/

# Wikify package runtime artifacts
node_modules/
dist/

# Secrets — never commit (keys, tokens, backend URLs, local env)
.env
.env.local
.env.*.local
**/.temp/
# wikify:end
