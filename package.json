{
  "name": "@invariance/wikify",
  "version": "0.1.0",
  "description": "Repo-native memory for humans and AI coding agents.",
  "type": "module",
  "bin": {
    "wikify": "bin/wikify.js"
  },
  "files": [
    "bin",
    "dist",
    "install.sh",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tmp=$(mktemp -d) && npm run build && node bin/wikify.js --help && node bin/wikify.js init --root \"$tmp\" --no-pr-history && node bin/wikify.js health --root \"$tmp\"",
    "prepublishOnly": "tsc"
  },
  "keywords": [
    "wiki",
    "repo-memory",
    "ai-agents",
    "claude-code",
    "codex",
    "cursor"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
